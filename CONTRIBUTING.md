# Contributing

Thanks for helping improve lakeboard-to-xmind.

## Local setup

```bash
python -m pip install -e .
python -m pip install pytest
python -m pytest
```

## Useful test cases

Please include small `.lakeboard` samples when reporting conversion issues. If a specific XMind build rejects an output file, include a known-good `.xmind` template produced by that build when possible.
